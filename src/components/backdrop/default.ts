export interface BackdropClasses {
  base: string
}
export const defaultBackdropClasses: BackdropClasses = {
  base: 'fixed inset-0 z-40 flex items-end bg-black bg-opacity-50 sm:items-center sm:justify-center',
}
